[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agents-md-generator"
version = "2.1.1"
description = "A Codex-ready skill for generating and maintaining scoped AGENTS.md rules"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
    { name = "Jiyuan Liu" },
    { name = "He Li" },
]
keywords = ["Codex", "AGENTS.md", "repository governance"]

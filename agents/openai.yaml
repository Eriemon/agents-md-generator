interface:
  display_name: "AGENTS.md Generator"
  short_description: "For the current workspace/current repository, treat 计划/规划/准备 as root AGENTS.md check entries before normal AGENTS work"
  default_prompt: "Use $agents-md-generator when the user explicitly asks about AGENTS.md or when the user is talking about the current workspace/current repository/current work folder and says 计划, 规划, or 准备. First inspect the current work folder root AGENTS.md state. If it is healthy, only report that the root AGENTS.md check passed. If it is missing, lacks version metadata, or is version-mismatched, report the exact problem and ask whether to enter the AGENTS.md design/restructure flow."

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agents-md-generator"
version = "2.1.0"
description = "Governed generation and verification of AGENTS.md repository rules"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Codex Maintainers" }]
keywords = ["Codex", "AGENTS.md", "repository governance"]

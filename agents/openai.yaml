interface:
  display_name: "AGENTS.md Generator"
  short_description: "Create, update, review, and verify repository AGENTS.md files"
  default_prompt: "Use $agents-md-generator to inspect this repository and create, update, review, or verify its scoped AGENTS.md guidance."
policy:
  allow_implicit_invocation: true
